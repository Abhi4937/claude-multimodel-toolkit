---
description: Token-hygiene checklist — which idle MCPs to disable, then run /context
---

Print the MCP keep/disable guidance for the current task type, then remind me to run `/context`.

- Keep: **serena**; **tradingview** (trading tasks); **obsidian** (note tasks).
- Disable via `/mcp` for this task: Adobe, Canva, Postman, Gmail, Calendar, Google Drive,
  Playwright/Chrome, and any language LSP not in use.
- Also remind me: `/clear` between unrelated tasks; `/rewind` (not `/compact`) to abandon a dead path.

Then tell me to run `/context` to confirm the window is lean, and flag any MCP that looks
idle-but-loaded. Do not disable anything yourself — just advise.
