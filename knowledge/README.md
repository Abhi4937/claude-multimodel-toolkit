# knowledge/ — durable cross-terminal facts

Permanent decisions, specs, and API notes that ALL terminals (opus / claude-glm / claude-vertex / agy) may need.
One fact per file. Link related notes with `[[name]]`.

This is the shared layer. Opus-only working notes stay in Claude's native memory
(`~/.claude/projects/.../memory/`), NOT here.

Discipline: read a **specific** file by path. Never scan the whole vault.
